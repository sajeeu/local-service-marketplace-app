{
  "name": "@local-service-marketplace/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API envelope types and error codes for the Local Service Marketplace",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
