// Prisma foundation schema.
// No business domain models in Phase 0 — Identity/Users and later domains arrive in Phase 1+.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
